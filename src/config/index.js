export const TIMEOUT = 30000;

export const DEFAULT_PAGE = {
  docs: [],
  total: 0,
  limit: 0,
  page: 0,
  pages: 0,
  sort: null
};

export const DEFAULT_LIMITS = [5, 10, 100, 500, 1000];

export const DEFAULT_LIMIT = 10;

export const HOST = `${window.location.protocol}//${window.location.host}`;
